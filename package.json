{
  "name": "shuangyue-about",
  "private": true,
  "type": "module",
  "scripts": {
    "optimize:images": "node scripts/optimize-images.mjs",
    "build:pages": "node scripts/build-pages.mjs",
    "build:worker": "npm run optimize:images && npm run build:pages && node scripts/prepare-worker-assets.mjs",
    "deploy": "npm run build:worker && npx wrangler pages deploy public --project-name about --branch main",
    "verify:images": "node scripts/verify-image-assets.mjs",
    "serve": "npx serve ."
  },
  "dependencies": {
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "wrangler": "^4.102.0"
  }
}
