{
  "name": "shuangyue-about",
  "private": true,
  "type": "module",
  "scripts": {
    "optimize:images": "node scripts/optimize-images.mjs",
    "build:pages": "node scripts/build-pages.mjs",
    "build": "npm run optimize:images && npm run build:pages && node scripts/prepare-worker-assets.mjs",
    "build:worker": "npm run build",
    "check": "node --check assets/js/app.js && node --check assets/js/admin.js && node --check assets/js/admin-d1.js && node --check assets/js/data.js && node --check worker.js && npm run verify:images && npm run check:data && npm run check:i18n && npm run check:links && npm run check:seo",
    "verify:images": "node scripts/verify-image-assets.mjs",
    "check:data": "node scripts/check-data.mjs",
    "check:i18n": "node scripts/check-i18n.mjs",
    "check:links": "node scripts/check-links.mjs",
    "check:seo": "node scripts/check-seo.mjs",
    "test:site": "node scripts/test-site.mjs",
    "serve": "npx serve ."
  },
  "dependencies": {
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "wrangler": "^4.102.0"
  }
}
