{
  "name": "shuangyue-about",
  "private": true,
  "type": "module",
  "scripts": {
    "build:pages": "node scripts/build-pages.mjs",
    "build:worker": "npm run build:pages && node scripts/prepare-worker-assets.mjs",
    "deploy": "npm run build:worker && npx wrangler deploy",
    "serve": "npx serve ."
  }
}
