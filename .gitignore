.wrangler/
.wrangler-dryrun/
node_modules/

# Build artifacts. Both are regenerated by `npm run build`, and the Cloudflare
# Pages project builds with `npm run build` into `public/`, so neither belongs in
# git: tracking them produced hundreds of files of diff on every build.
/public/
/assets/images/generated/

# Local agent workspace state
.reasonix/
.workbuddy/
